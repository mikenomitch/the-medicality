---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en" id="top">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main>
      <section class="hero" aria-labelledby="hero-title">
        <div class="hero-media" role="img" aria-label="Luxurious, calm healthcare lounge rendered as a pencil sketch."></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
          <p class="eyebrow">Patient experience reimagined</p>
          <h1 id="hero-title">Five-Star Hospitality for Healthcare</h1>
          <p class="subtitle">Transforming patient care with ultra-luxury service standards and medical expertise.</p>
          <div class="hero-actions">
            <a class="btn primary" href="#contact">Schedule a Consultation</a>
            <a class="btn ghost" href="#difference">Discover the Difference</a>
          </div>
        </div>
      </section>

      <section class="intro" id="overview">
        <p class="eyebrow">Welcome to The Medicality</p>
        <div class="intro-card">
          <p>
            The Medicality is a patient care consulting firm that blends the art of luxury hospitality with the science of
            healthcare. We partner with hospitals to transform traditional clinical environments into five-star,
            patient-centered experiences. By leveraging world-class service principles from ultra-luxury hotels, we elevate
            every moment of the patient journey while empowering your caregivers. The result is exceptional care delivered
            with unparalleled service, enhancing satisfaction, staff pride, and reputation.
          </p>
        </div>
      </section>

      <section class="difference" id="difference">
        <div class="section-header">
          <p class="eyebrow">Our unique approach</p>
          <h2>The Medicality Difference</h2>
          <p class="lede">We immerse ourselves in your culture and redefine service from within.</p>
        </div>
        <div class="pillars">
          <article class="pillar">
            <div class="icon-circle">❤</div>
            <h3>Dual Focus on People</h3>
            <p>
              We elevate patient satisfaction by first uplifting your care teams. By treating employee morale and culture as
              the foundation, five-star service becomes effortless at every level.
            </p>
          </article>
          <article class="pillar">
            <div class="icon-circle">∞</div>
            <h3>Embedded Transformation</h3>
            <p>
              Our experts embed within your hospital for 6–12 months, implementing new systems and behaviors in real-time.
              We become part of your team to guide lasting cultural change.
            </p>
          </article>
          <article class="pillar">
            <div class="icon-circle">★</div>
            <h3>Five-Star Service Standards</h3>
            <p>
              We introduce proven hospitality protocols—personalized greetings, confident non-verbal cues, immediate service
              recovery—so patients feel the warmth of a luxury destination.
            </p>
          </article>
        </div>
      </section>

      <section class="services" id="services">
        <div class="section-header">
          <p class="eyebrow">What we do</p>
          <h2>Our Services</h2>
          <p class="lede">Comprehensive consulting to transform both patient experience and workplace culture.</p>
        </div>
        <div class="service-grid">
          <article class="service-card">
            <div class="sketch-box">Process Map</div>
            <h3>Hospitality-Inspired Process Redesign</h3>
            <p>
              We overhaul key processes from pre-arrival to post-discharge, infusing best practices from luxury hospitality
              at every step of the journey.
            </p>
          </article>
          <article class="service-card">
            <div class="sketch-box">Empathy Lab</div>
            <h3>Empathy-Driven Staff Training</h3>
            <p>
              Organization-wide training for executives and frontline teams focused on empathy, communication, and
              service excellence.
            </p>
          </article>
          <article class="service-card">
            <div class="sketch-box">Culture Canvas</div>
            <h3>Employee Experience Consulting</h3>
            <p>
              We strengthen the employee lifecycle—from hiring to growth—building a culture where caregivers feel valued,
              supported, and inspired.
            </p>
          </article>
        </div>
        <div class="cta-banner">
          <div>
            <p class="eyebrow">Ready to begin?</p>
            <h3>Bring five-star service to your hospital.</h3>
          </div>
          <a class="btn primary" href="#contact">Start the conversation</a>
        </div>
      </section>

      <section class="contact" id="contact">
        <div class="section-header">
          <p class="eyebrow">Request a consultation</p>
          <h2>Contact Us</h2>
          <p class="lede">Ready to elevate your patient experience? Let's discuss how The Medicality can guide your transformation.</p>
        </div>
        <div class="contact-grid">
          <form class="contact-form">
            <label>
              Name
              <input type="text" name="name" placeholder="Your name" required />
            </label>
            <label>
              Email
              <input type="email" name="email" placeholder="name@company.com" required />
            </label>
            <label>
              Organization
              <input type="text" name="organization" placeholder="Hospital or health system" />
            </label>
            <label class="full-row">
              Message
              <textarea name="message" rows="4" placeholder="Tell us about your goals"></textarea>
            </label>
            <button type="submit" class="btn primary">Send Message</button>
            <p class="privacy">We will never share your information. Expect a response within 1 business day.</p>
          </form>
          <div class="contact-notes">
            <div class="note-card">
              <h3>Direct Contact</h3>
              <p>Email: info@themedicality.com</p>
              <p>Phone: +1 (000) 000-0000</p>
              <p>Office: 112 Maisonette Way, Suite 14, New York, NY</p>
            </div>
            <div class="note-card muted">
              <h3>Our Promise</h3>
              <p>Thoughtful partnership, discreet guidance, and measurable lift in patient and employee satisfaction.</p>
            </div>
          </div>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>

<style>
  .hero {
    position: relative;
    height: 78vh;
    min-height: 620px;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: var(--shadow-soft);
    margin-top: 1.5rem;
    border: 1px solid var(--outline);
    background: #efe7db;
  }

  .hero-media {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(120deg, rgba(15, 43, 56, 0.18), rgba(247, 242, 233, 0.1)),
      url('https://images.unsplash.com/photo-1508387022045-5a381ceba0aa?auto=format&fit=crop&w=1900&q=80');
    background-size: cover;
    background-position: center;
    filter: grayscale(100%) contrast(1.1) brightness(0.9);
    transform: scale(1.02);
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(120deg, rgba(15, 43, 56, 0.45) 10%, rgba(15, 43, 56, 0.12) 50%, rgba(247, 242, 233, 0.55) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 600px;
    padding: 4.5rem 4rem;
    color: #f7f2e9;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.78rem;
    color: var(--accent);
    margin: 0;
  }

  .subtitle {
    font-size: 1.15rem;
    color: #f7f2e9;
    max-width: 520px;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 0.5rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
    padding: 0.85rem 1.4rem;
    border-radius: 999px;
    border: 1px solid transparent;
    font-weight: 600;
    letter-spacing: 0.04em;
    cursor: pointer;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background 0.2s ease, color 0.2s ease;
  }

  .btn.primary {
    background: var(--accent-dark);
    color: #f7f2e9;
    border-color: var(--accent-dark);
    box-shadow: 0 10px 24px rgba(15, 43, 56, 0.18);
  }

  .btn.primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 14px 32px rgba(15, 43, 56, 0.2);
  }

  .btn.ghost {
    border-color: #f7f2e9;
    color: #f7f2e9;
    background: rgba(247, 242, 233, 0.1);
  }

  .btn.ghost:hover {
    background: rgba(247, 242, 233, 0.2);
    transform: translateY(-2px);
  }

  .intro {
    margin: 4rem 0 3rem;
  }

  .intro-card {
    border: 1px solid var(--outline);
    background: rgba(255, 255, 255, 0.65);
    padding: 1.6rem 1.8rem;
    border-radius: 18px;
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.5), var(--shadow-soft);
  }

  .section-header {
    margin-bottom: 1.5rem;
  }

  .section-header .lede {
    max-width: 640px;
    color: var(--muted);
  }

  .pillars {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.4rem;
  }

  .pillar {
    background: rgba(255, 255, 255, 0.75);
    border: 1px dashed rgba(31, 27, 24, 0.28);
    border-radius: 16px;
    padding: 1.4rem 1.4rem 1.6rem;
    box-shadow: 0 8px 22px rgba(15, 43, 56, 0.06);
    position: relative;
  }

  .icon-circle {
    width: 44px;
    height: 44px;
    border: 1px solid rgba(31, 27, 24, 0.35);
    border-radius: 50%;
    display: grid;
    place-items: center;
    font-size: 1rem;
    color: var(--accent-dark);
    background: #f1e8d8;
    margin-bottom: 0.9rem;
  }

  .services {
    margin: 4rem 0 3rem;
  }

  .service-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .service-card {
    background: rgba(255, 255, 255, 0.82);
    border: 1px solid var(--outline);
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: var(--shadow-soft);
    display: grid;
    gap: 0.8rem;
  }

  .sketch-box {
    border: 1.5px dashed rgba(31, 27, 24, 0.5);
    border-radius: 12px;
    padding: 0.9rem 1rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: 'Cormorant Garamond', 'Times New Roman', serif;
    letter-spacing: 0.08em;
    color: var(--accent-dark);
    background: linear-gradient(135deg, rgba(181, 154, 99, 0.1), rgba(15, 43, 56, 0.06));
    box-shadow: inset 0 2px 6px rgba(0, 0, 0, 0.04);
  }

  .cta-banner {
    margin-top: 2.2rem;
    padding: 1.6rem 1.8rem;
    border-radius: 16px;
    border: 1px solid var(--outline);
    background: rgba(15, 43, 56, 0.85);
    color: #f4eee0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    box-shadow: var(--shadow-soft);
  }

  .cta-banner h3 {
    margin: 0.1rem 0 0;
    color: #fdfaf4;
  }

  .cta-banner .btn {
    white-space: nowrap;
  }

  .contact {
    margin: 4rem 0;
  }

  .contact-grid {
    display: grid;
    grid-template-columns: 3fr 2fr;
    gap: 1.5rem;
  }

  .contact-form {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem 1.2rem;
    padding: 1.6rem;
    border-radius: 16px;
    border: 1px solid var(--outline);
    background: rgba(255, 255, 255, 0.85);
    box-shadow: var(--shadow-soft);
  }

  .contact-form label {
    display: grid;
    gap: 0.35rem;
    font-size: 0.95rem;
    color: var(--graphite);
  }

  .contact-form input,
  .contact-form textarea {
    width: 100%;
    padding: 0.75rem 0.9rem;
    border: 1px solid var(--outline);
    border-radius: 10px;
    background: #fdfaf4;
    font-size: 1rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .contact-form input:focus,
  .contact-form textarea:focus {
    outline: none;
    border-color: var(--accent-dark);
    box-shadow: 0 0 0 3px rgba(15, 43, 56, 0.08);
  }

  .contact-form textarea {
    resize: vertical;
  }

  .contact-form .full-row {
    grid-column: 1 / -1;
  }

  .contact-form .btn {
    grid-column: 1 / -1;
    width: fit-content;
  }

  .privacy {
    grid-column: 1 / -1;
    font-size: 0.9rem;
    color: var(--muted);
    margin: 0;
  }

  .contact-notes {
    display: grid;
    gap: 1rem;
  }

  .note-card {
    padding: 1.4rem 1.3rem;
    border-radius: 14px;
    border: 1px dashed rgba(31, 27, 24, 0.32);
    background: rgba(255, 255, 255, 0.7);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.6), 0 8px 18px rgba(15, 43, 56, 0.05);
  }

  .note-card.muted {
    background: rgba(181, 154, 99, 0.08);
    border-style: solid;
  }

  @media (max-width: 960px) {
    .hero {
      height: auto;
      min-height: 540px;
    }

    .hero-content {
      padding: 3.2rem 2.5rem;
      max-width: 520px;
    }

    .contact-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .hero-content {
      padding: 2.8rem 2.1rem;
    }

    .btn {
      width: 100%;
      justify-content: center;
    }

    .service-grid,
    .pillars {
      grid-template-columns: 1fr;
    }

    .contact-form {
      grid-template-columns: 1fr;
    }

    .cta-banner {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
